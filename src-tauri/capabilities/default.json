{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "前端只读取系统光标；窗口铺设由 Rust 原生侧完成。",
  "windows": [
    "main",
    "wallpaper-*"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-cursor-position"
  ]
}
